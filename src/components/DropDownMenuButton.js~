import React from "react";
import Radium from "radium";


//TODO
//make work
//make it so that it can collapse back down
@Radium
export default class DropDownMenuButton extends React.Component {
  constructor(props) {
    super(props);
    this.state = {expanded: false};
  }
  render() {
    return(
      <div>
          <button style={[styles.base, {fontFamily: "sans-serif", width: 75.88, backgroundColor: "gray", color: "white", marginLeft: "3px"}} onClick={()=>{this.setState({expanded: true});}}>More</button>
          {this.state.expanded ? <Menu items={items} onClick={onClick}/> : null}
      </div>
    );
  }
}
